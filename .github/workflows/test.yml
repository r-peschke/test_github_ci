name: Validate models.yml and initial and example data
on: [push, pull_request]
env:
  PYTHON_VERSION: 3.9.6
jobs:
  job1:
    name: Name Job 1
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Setup python
      uses: actions/setup-python@v2
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: cat this file
      run: cat .github/workflow/test.yml

  job2:
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v2
      
    - name: env
      run: env
      
    - name: list
      run: ls

    - name: show readme.md
      run: cat README.md

    - name: Protect README.md
      run: echo "2497b778264ccfb2d98bd6a01d6eda62 README.md" | md5sum -c -
